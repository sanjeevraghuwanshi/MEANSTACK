import { RollOverDirective } from './roll-over.directive';

describe('RollOverDirective', () => {
  it('should create an instance', () => {
    const directive = new RollOverDirective();
    expect(directive).toBeTruthy();
  });
});
